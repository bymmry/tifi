# - * -coding: UTF - 8 - * -


def createPlaylist(Playlist):
    # albumSchema = Playlist(wyID=album['wyID'])
    # albumSchema.save()
    # return jsonify({'code': 200, 'info': '成功添加专辑', 'albums': postback})